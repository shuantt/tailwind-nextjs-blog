# AGENTS.md

> **開始任何工作之前，請先完整閱讀根目錄的 [`CLAUDE.md`](./CLAUDE.md)。**

本 repository 對所有 AI coding agents（Codex、Cursor、Claude Code 及其他工具）的完整規範都在
`CLAUDE.md`，那是唯一的 source of truth。本檔案只是指標，不包含任何規則。

1. 先讀完 `CLAUDE.md`，並依其內容行事，包含「一律使用繁體中文（台灣用語）回應使用者」。
2. 若本檔與 `CLAUDE.md` 有出入，以 `CLAUDE.md` 為準。
3. 不要在本檔新增規則；需要新增或修改規範時，請改 `CLAUDE.md`。

---

_For agents that only load `AGENTS.md`: all repository instructions live in `CLAUDE.md` at the
repository root. Read it in full before doing anything, treat it as the single source of truth, and
reply to the user in Traditional Chinese (zh-TW)._
